{"commit":"541e062","id":"H1","severity":"high","status":"done","kind":"code","source":"PRODUCTION.md","title":"Server bundles ship React's development builds — 2x every target","files":["packages/core/src/builder/env-shadow-loader.cjs","packages/core/src/builder/rspack-config.ts"],"fix":"Substitute process.env.NODE_ENV in the SSR layer before the env prelude shadows the binding, so DefinePlugin's dead-code elimination works again. -50.7% on node.","notes":"Superseded by F8/G2: the substitution is now a scanner that skips comments, strings and templates."}
{"commit":"ed47ada","id":"M1","severity":"medium","status":"done","kind":"docs","source":"PRODUCTION.md","title":"JSDoc prescribes useNavigation().url for a static route's query — it is the same frozen URL","files":["packages/core/src/router.ts"],"fix":"Correct the PageProps.url JSDoc: on a static route both PageProps and useNavigation() carry the build-time URL; render: 'dynamic' or a location-reading effect are the only ways to see the query.","notes":"The README was not affected — a one-JSDoc fix."}
{"commit":"550a59c","id":"M2","severity":"medium","status":"done","kind":"code+docs","source":"PRODUCTION.md","title":"Any cross-site form POST to a page route is refused, message blames a server action","files":["packages/core/src/runtime/entry.rsc.tsx","packages/core/README.md"],"fix":"Reword the 403 to name the real constraint (a page route cannot accept a cross-site form post; use { type: 'endpoint' }) and add the limitation to the README.","notes":"Refusing before parsing is correct — the body cannot be trusted to buffer."}
{"commit":"0950b1e","id":"M3","severity":"medium","status":"done","kind":"code","source":"PRODUCTION.md","title":"A malformed server-action body is a 500 + error-tracker page, not a 400","files":["packages/core/src/runtime/entry.rsc.tsx"],"fix":"Wrap the body read and decode so an undecodable action body answers 400 and never reaches reportServerError. Action ids are public, so this was an unauthenticated 500 vector.","notes":"Follow-ups F3, F4 and F6 all came out of this fix."}
{"commit":"08fd176","id":"M4","severity":"medium","status":"done","kind":"docs","source":"PRODUCTION.md","title":"notFound() always costs a full document load on a soft navigation","files":["packages/core/README.md"],"fix":"State that a soft navigation has no shell to beat, so every notFound() costs a document reload, and that redirect() does not.","notes":"Deliberate and self-healing via reloadOnceForLateNotFound(); only the cost was undocumented."}
{"commit":"d30d0e5","id":"L1","severity":"low","status":"done","kind":"code","source":"PRODUCTION.md","title":"Two shapes of dead route pass the shadow check (/u/:id vs /u/:name, /a/* ahead of /a/b)","files":["packages/core/src/runtime/validate-entries.ts"],"fix":"Key assertNothingIsShadowed on the normalised pattern rather than the literal path string, and make the key wildcard-aware.","notes":"F10 records where the normalisation stops: two equivalent {regex} constraints still both pass."}
{"commit":"ad4c261","id":"L2","severity":"low","status":"done","kind":"code","source":"PRODUCTION.md","title":"An unknown CLI flag prints a raw Node stack trace","files":["packages/core/src/cli/index.ts"],"fix":"Wrap parseArgs and print \"rshono: <message>\" plus HELP, matching the other bad-input paths.","notes":"F1 found the same raw-stack shape for [rshono] build/dev failures."}
{"commit":"f4783b9","id":"L3","severity":"low","status":"done","kind":"docs","source":"PRODUCTION.md","title":"HOST is read by the CLI, then silently dropped by rshono dev","files":["packages/core/README.md","packages/core/src/cli/index.ts"],"fix":"Scope the README line to `start` and warn when HOST is set under `dev`. Binding dev to loopback stays — dev source maps embed 'use server' source.","notes":null}
{"commit":"dace1f2","id":"L4","severity":"low","status":"done","kind":"code","source":"PRODUCTION.md","title":"Prerendered documents bake in a build-time CSP nonce","files":["packages/core/src/runtime/entry.rsc.tsx","packages/core/src/server/ssg.ts"],"fix":"Have the prerender pass ask for a document with no nonce, the way it already asks for the RSC variant.","notes":"F2 fixed the other half: under a global nonce policy the document is never served, and the build now says so."}
{"commit":"b86a3f5","id":"L5","severity":"nit","status":"done","kind":"polish","source":"PRODUCTION.md","title":"Five smaller things: env snapshot doc, readBuildMarker type, decodeAction arity, .d.ts.map, coverage gate scope","files":["packages/core/src/runtime/context.ts","packages/core/src/deploy/build-marker.ts","packages/core/src/types/react-server-dom-rspack.d.ts","packages/core/tsconfig.build.json","packages/core/package.json"],"fix":"All five taken: document that ctx.env is per-process, validate against DEPLOY_TARGETS, drop the invented serverManifest parameter, drop declarationMap, and say what the coverage gate does not cover.","notes":"The coverage half became F7, which measured the hot path the gate cannot see."}
{"commit":"49e1c9b","id":"F1","severity":"medium","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"A [rshono] failure from dev or build still prints a raw Node stack","files":["packages/core/src/cli/index.ts","packages/core/src/cli/build.ts","packages/core/src/cli/dev.ts"],"fix":"Collapse phase() into main().catch: a message starting with [rshono] is printed as the line it is, anything else keeps its stack. Covers every command.","notes":"Origin: found via L2. Two tests now also assert no stack."}
{"commit":"0b802a5","id":"F2","severity":"low","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"Under a global nonce CSP, prerendered documents are built and never served","files":["packages/core/src/runtime/entry.rsc.tsx","packages/core/src/server/ssg.ts"],"fix":"entry.rsc.tsx sets x-rshono-prerender-nonce on a document rendered while secureHeadersNonce was set (only under RSHONO_PRERENDER); the build labels those pages 'flight only'. The documents are still written, since the policy is a request-time decision.","notes":"Residue of L4."}
{"commit":"ebe9563","id":"F3","severity":"low","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"A deploy-drift loadServerAction failure is now a silent 400","files":["packages/core/src/runtime/entry.rsc.tsx"],"fix":"Split M3's guard: body read/decode still answers 400, loadServerAction gets its own catch that reports source: 'action' and re-throws, so the app's error page answers 500. The hasOwn check above makes the split safe.","notes":"Cost of M3. G1 made this worth more than it looked: a partial deploy loses every action, not one."}
{"commit":"f25ab48","id":"F4","severity":"medium","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"decodeFormState sits outside M3's guard — an unauthenticated 500 after the action ran","files":["packages/core/src/runtime/entry.rsc.tsx"],"fix":"Wrap decodeFormState; formState stays undefined (what a form without useActionState gets) and the page renders 200 with the action's effects intact. Silent in production, console.warn in dev.","notes":"Filed low, raised by the reproduction: the two decoders disagree about which $ACTION_ field names the call. Needed a new /subscribe fixture — a server component wiring a form straight to an action."}
{"commit":"41150cf","id":"F5","severity":"nit","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"The two action 400s carry no cache-control and no Vary: RSC","files":["packages/core/src/runtime/entry.rsc.tsx"],"fix":"One plainRefusal(c, message, status) for every plain-text answer on a page route: both 404s, both action 400s, the cross-site 403 and the last-resort 500.","notes":"Consistency, not correctness — RFC 9111 does not let a shared cache store a 400 heuristically."}
{"commit":"49dca49","id":"F6","severity":"nit","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"The client's \"produced no result\" branch is now close to unreachable","files":["packages/core/src/runtime/entry.client.tsx"],"fix":"Superseded by F3, which gave the branch a reachable cause again: a partial deploy answers a client-initiated action with the error page as a flight payload carrying no returnValue. Comment corrected; covered server-side in prod.test.mjs and client-side by a new Playwright test.","notes":"Residue of M3."}
{"commit":"ca93b80","id":"F7","severity":"low","status":"done","kind":"docs","source":"PRODUCTION-FOLLOWUPS.md","title":"The coverage number still cannot see the request hot path","files":["packages/core/README.md",".github/workflows/ci.yml"],"fix":"Measured with NODE_V8_COVERAGE on the child processes and c8 report: entry.rsc.tsx 97.4%, entry.ssr.tsx 100%, navigation.tsx 100%. Recipe written down rather than made a gate — merging it needs devtoolModuleFilenameTemplate changes that would alter shipped maps.","notes":"The code the gate cannot see is not the code that is thin."}
{"commit":"243fc74","id":"F8","severity":"nit","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUPS.md","title":"H1's NODE_ENV substitution is textual, not syntactic","files":["packages/core/src/builder/env-shadow-loader.cjs"],"fix":"A scanner that matches comments, strings and templates ahead of the target and hands back anything that is not code. Comments are matched before strings so an apostrophe in a comment cannot open a string.","notes":"Residual: a regex literal with unescaped dots. Any position the scan gets wrong leaves the text as the bare replace() did."}
{"commit":"28f346d","id":"F9","severity":"nit","status":"done","kind":"polish","source":"PRODUCTION-FOLLOWUPS.md","title":"prettier --check fails on five files and nothing in CI runs it","files":["package.json",".github/workflows/ci.yml",".prettierignore"],"fix":"format:check as a root script and a lint-job step. Three files formatted; two generated ones ignored, since a formatter and a generator arguing over the same bytes is how a check starts being ignored.","notes":null}
{"commit":"8523120","id":"F10","severity":"nit","status":"done","kind":"docs","source":"PRODUCTION-FOLLOWUPS.md","title":"The shadow check compares regex constraints as text","files":["packages/core/src/runtime/validate-entries.ts"],"fix":"Documented as a deliberate limit, with which way it must lean: an unreachable route slipping through is acceptable, a live route wrongly refused is not. Pinned with a test.","notes":"Limit of L1."}
{"commit":"cba5fc8","id":"G1","severity":"low","status":"done","kind":"code","source":"PRODUCTION-FOLLOWUP-FOLLOWUPS.md","title":"One unloadable 'use server' module disables every server action in the app","files":["packages/core/src/builder/check-app-modules.ts","packages/core/README.md"],"fix":"The bundling is upstream and stays. checkRouteModules became checkAppModules and now loads the app's server actions at build time, one loadServerAction per module, and warns (exit 0) if one will not evaluate.","notes":"Found while building F3's fixture. Rspack concatenates every 'use server' module into one server module."}
{"commit":"abd31b8","id":"G2","severity":"nit","status":"done","kind":"docs","source":"PRODUCTION-FOLLOWUP-FOLLOWUPS.md","title":"H1's justification for a textual substitution was wrong about DefinePlugin","files":["PRODUCTION.md","packages/core/src/builder/env-shadow-loader.cjs"],"fix":"Struck the claim in H1 and measured the truth: DefinePlugin works on the parsed module and cannot reach inside a string literal. A throwaway app proves both bundles preserve the string and inline the read.","notes":"\"Someone else already takes this risk\" is what got F8 filed as a nit."}
{"commit":"b714968","id":"G3","severity":"low","status":"done","kind":"process","source":"PRODUCTION-FOLLOWUP-FOLLOWUPS.md","title":"The browser suite has never run in this environment, and now has a new test in it","files":["packages/core/test/browser/client-runtime.spec.mjs"],"fix":"The maintainer ran test:browser outside the sandbox and every test passed, F6's new one included. Chromium still segfaults here, so anything written for that suite in-sandbox is written blind.","notes":"Environment, not a defect."}
{"commit":"644305d","id":"C1","severity":"medium","status":"done","kind":"code+docs","source":"PRODUCTION-CORE-REVIEW.md","title":"The app's error page is never used when a page component throws — the commonest 500","files":["packages/core/src/runtime/entry.ssr.tsx","packages/core/src/router.ts"],"fix":"renderHTML re-throws the shell failure instead of absorbing it into a framework document, so the fault reaches app.onError and the error page answers from a fresh render; and onError's HTML branch emits failureDocument, moved to its own module, so an app with no error page keeps a visible 500.","notes":"Two parts, and the second is not optional — the one-line version hands a browser text/plain and the suite stays green. Report count is unchanged at two (render, ssr): the WeakSet absorbs onError's report because onShellError reported the same object first. That second report is C2. The flight half still answers 200 (C8)."}
{"commit":"91ecbca","id":"C2","severity":"medium","status":"done","kind":"code","source":"PRODUCTION-CORE-REVIEW.md","title":"One fault, two reports: render and then ssr","files":["packages/core/src/runtime/entry.ssr.tsx"],"fix":"One named predicate, cameFromPayload(error), and the digest test made in the two places that lacked it: the shell path in entry.ssr.tsx and app.onError in entry.rsc.tsx. One fault is now reported once, tagged by the stage that recognised it.","notes":"Both guards are needed — with only the shell one the duplicate moves from 'ssr' to 'request', since renderComponent re-throws the stand-in (C1). onShellError is now a defensive floor for a rejection React never announced, and its doc says so. prod.test.mjs's report test names 'render' and asserts the absence of the other two; a new testbed fixture (/crash?ssr=1, a 'use client' component that throws on the server) keeps the 'ssr' source reachable, so this cannot silently kill it."}
{"commit":"a58f844","id":"C3","severity":"low","status":"done","kind":"code","source":"PRODUCTION-CORE-REVIEW.md","title":"A redirect() from the error page is swallowed, and reported as a failure","files":["packages/core/src/runtime/entry.rsc.tsx"],"fix":"One line in the error page's catch: a RedirectSignal is answered through respondToControlSignal, the same branch the notFound page's catch already has. notFound() from the error page stays reported.","notes":"The testbed's 500.tsx gained the ?boom= twin of its 404 page, so both signals are exercised from all three paths that render the error page. The flight half is unaffected and asserted as such: an error page rendered as a payload commits its 500 before the signal arrives, so the redirect rides the digest — two representations, two mechanisms, which is worth having pinned. An error page redirecting to a URL that also fails is now a browser-ended loop where it was a 500; the app's own mistake, and the notFound page can already make it."}
{"commit":"4ad8079","id":"C4","severity":"low","status":"done","kind":"code","source":"PRODUCTION-CORE-REVIEW.md","title":"A route under the reserved /_static prefix builds cleanly and can never run","files":["packages/core/src/runtime/validate-entries.ts"],"fix":"assertNothingIsShadowed now checks the framework's own mount first: a route whose path is /_static or sits below it is refused by name. Only a literal path — a parameterised route that overlaps the prefix still answers everything else.","notes":"Runs at every server start, not only at build, so an app booting today with a dead /_static route refuses to boot after an upgrade. Deliberate, and in the README's limitations list. RESERVED_PREFIX is written out in validate-entries.ts rather than imported: the mounts live in deploy/filesystem.ts, deploy/cloudflare/runtime.ts and cli/dev.ts, none of which the RSC layer can pull in. Changelog entry still owed."}
{"commit":"76fa202","id":"C5","severity":"low","status":"done","kind":"docs","source":"PRODUCTION-CORE-REVIEW.md","title":"rshono dev answers /_static itself, so app middleware never sees an asset request","files":["packages/core/src/cli/dev.ts","packages/core/README.md"],"fix":"Documented, not moved: the reasoning at the mount in cli/dev.ts, a line in the README's limitations list, and a dev test pinning the divergence (a page carries x-response-time, an asset does not).","notes":"Proxying assets would park them behind workerGate — the client bundle is built by a separate compiler, so a save touching one server component would stall the browser's JS. The narrower shape, if it ever has to go: proxy /_static only while a worker is live. The test is the guard: change the mount and it fails, which sends the reader to the comment."}
{"commit":"06828e5","id":"C6","severity":"low","status":"done","kind":"portability","source":"PRODUCTION-CORE-REVIEW.md","title":"ssgFilePath accepts segment names Windows cannot store","files":["packages/core/src/server/prerendered.ts"],"fix":"Two clauses in isStorableSegment: RESERVED_DEVICE_NAME (Microsoft's list, case-insensitive, extension or not) and TRAILING_STRIPPED (a trailing dot or space). The build message names all three families, and staticPaths' JSDoc states the rule.","notes":"Fails builds that work today, on purpose — the same trade : and * already made. The false-refusal direction is what the tests guard: console, comic, contents, nullable, v1.2 and 'a b' all still resolve. Still not observed on Windows; this sandbox is macOS, so it is the mapping plus the rule, not a run."}
{"commit":"8aa1773","id":"C7","severity":"nit","status":"done","kind":"code","source":"PRODUCTION-CORE-REVIEW.md","title":"Two CLI failure paths exit without draining","files":["packages/core/src/cli/start.ts","packages/core/src/cli/index.ts"],"fix":"start.ts's two refusals and dev.ts's port-in-use refusal return exit(1); readPort is gone, its parse inlined in main where the exit can be awaited. A structural test pins that nothing in dist/cli calls process.exit except exit.js.","notes":"The review named three paths; dev.ts's port-in-use was a fourth, which is why the test is structural rather than three spawns. The trap avoided: an async readPort would make readPort(a) ?? readPort(b) never-nullish, so PORT would stop being read. All four messages and exit codes verified unchanged against the real CLI."}
{"commit":null,"id":"C8","severity":"nit","status":"open","kind":"docs","source":"PRODUCTION-CORE-REVIEW.md","title":"A flight request for a throwing page answers 200 where the document answers 500","files":["packages/core/README.md"],"fix":"A sentence in the README's error-handling paragraph, beside the notFound() one it already has.","notes":"Resist \"fixing\" the status: buffering the payload to learn whether it failed would cost every page its streaming, which is the trade M4 settled."}
{"commit":null,"id":"C9","severity":"low","status":"open","kind":"code+docs","source":"PRODUCTION-CORE-REVIEW.md","title":"public/ shadows a page route on cloudflare and vercel, and not on node","files":["packages/core/src/deploy/contract.ts","packages/core/src/deploy/vercel/build.ts","packages/core/src/deploy/cloudflare/build.ts","packages/core/README.md"],"fix":"Correct mountPublicFallback's contract line (a fallback where the app owns the surface, a CDN-first store where the platform routes), and add a build-time warning on the two CDN targets.","notes":"Found while auditing C4; reasoning from the emitted configuration, not observed. The warning must compare what a request resolves to, not raw filenames — public/about.html answers /about.html and an index.html answers its directory, the same normalisation ssgFilePath does."}
