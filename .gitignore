node_modules/
dist/
# `rshono dev`'s build output, kept apart from `dist/` so the two commands cannot overwrite each other.
.rshono/
.vercel/
*.tsbuildinfo
.DS_Store
.env*
test-results/
playwright-report/
