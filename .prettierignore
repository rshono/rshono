node_modules/
dist/
build/
coverage/
pnpm-lock.yaml
yarn.lock
package-lock.json
.output/
.next/
# Committed fixture and its per-app copies: reformatting them changes the bytes every historical
# benchmark result was measured against.
packages/benchmarks/fixtures/data.json
packages/benchmarks/apps/*/src/generated/
# Generated by the TanStack Start router plugin.
packages/benchmarks/apps/tanstack-start/src/routeTree.gen.ts
# Written by Next.js on every build; formatting it lasts until the next one.
packages/benchmarks/apps/next/next-env.d.ts
# Copied out of `results/latest.md` by `pnpm --filter @rshono/benchmarks site:publish` — the same file as
# the ignored results above, one directory further on, so the formatter must not disagree with it either.
apps/website/content/benchmarks.md
# Regenerated by `pnpm --filter @rshono/benchmarks report`.
packages/benchmarks/results/
